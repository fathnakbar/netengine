import { init } from "./router.js";

// import ORM for resource fetch and filtering
import { tableName } from "";


export function $get(EndpointInstance) {
    const {request, response} = EndpointInstance;
    // Pagination
    // Get by id
    // Sort
}

export function $post(params) {

}

export function $delete(params) {
    
}

export function $put(params) {
    
}